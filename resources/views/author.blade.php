{{-- @extends('layouts.app')

@section('content')
<div class="container">
    <h2 class="text-item text-center mt-3 my-4">Author</h2>

    <h3>Posts:</h3>
    <ul>
        @foreach ($posts as $post)
            <li><a href="/Categories/{{ $post->name }}" class="text-decoration-none">{{ $post->title }}</a></li>
        @endforeach
    </ul>
</div>
@endsection --}}
